# Model related
alg_name: sta
layers: [20]
multipliers: [1.0]
trims: [0.65]  # Must match the trim used during generation
mode: act_and_freq  # Must match the mode used during generation (act_and_freq, only_act, or only_freq)
save_activations: True # set to True to save the activations of the model